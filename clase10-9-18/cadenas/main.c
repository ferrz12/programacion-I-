#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char cadena[20]="";
    int cantidad;

    cadena = "perro";

    //printf("Ingrese texto : ");
    //fflush(stdin);
    //scanf("%s", &cadena);
    //fgets(cadena, 20, stdin);

    //gets(cadena);

    //printf("%s",cadena);

    //cantidad = strlen(cadena);

    printf("%d", cantidad);

    return 0;
}
